# backend/engine/solene_engine.py

def generate_response(prompt: str) -> str:
    # No emotional softness—Solene speaks with directness
    formatted_prompt = f"[assertive]\n{prompt}"

    # Placeholder for OpenChat inference
    response = f"Solene replies boldly: '{formatted_prompt}'"
    return response